---
description: Pointer to AGENTS.md — CI hygiene for supervisor and workflows
globs: scripts/agent_supervisor.py,scripts/agent_prompts/**,AGENTS.md,.github/workflows/*.yml
alwaysApply: false
---

# Agent Orchestration

Canonical supervisor protocol lives in **`AGENTS.md`** (Scout → Implementer → Verifier → Reviewer → CI triage). Do not duplicate it here.

When editing supervisor scripts, prompts, or CI workflows:

## CI hygiene (release)

PR workflow `.github/workflows/quality.yml` — scoped ruff, engine cov, pip-audit; not a substitute for `VERIFY_COMMANDS`.

- Scoped lint: `python -m ruff check <paths>` (prefer scoped over `ruff check .`)
- Engine coverage: `pytest --cov=pipeline --cov=projection --cov=stratigraphy --cov=ingestion --cov-fail-under=70`
- Dependency audit: `pip-audit -r requirements.txt`
- E2E gate: **three** commands in `AGENTS.md` / `VERIFY_COMMANDS` (not the Streamlit UI’s four Prepare steps)

## Cross-references

- `AGENTS.md` — roles, routing, E2E gate, SDK flags
- `multi-agent-personas.mdc` — PM / Architect / Developer / QA / DevOps personas
- `borehole-platform-architecture.mdc` — always-on boundaries; `borehole-module-catalog.mdc` — full module table
- `pipeline-orchestration.mdc` — `build_cross_section()` contract
- `packaging-deployment.mdc` — Docker / PyInstaller / `ops_*` production hardening
